import hostname from './hostname.svg';
import deviceType from './device-type.svg';
import deviceUptime from './device-uptime.svg';
import systemTime from './system-time.svg';
import systemInfo from './system-info.svg';

// Định nghĩa và export đối tượng `iconDashboard` đúng
const iconDashboard = {
    hostname,
    deviceType,
    deviceUptime,
    systemTime,
    systemInfo
};

export default iconDashboard;
